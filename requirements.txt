httpx>=0.27,<1.0
pydantic>=2.0,<3.0
boto3>=1.34,<2.0
tenacity>=8.0,<9.0
python-dateutil>=2.8

#------Transitive security floors------
# Pulled in transitively (httpx/requests/botocore) but pinned here to keep
# the resolver above known-vulnerable releases.
# idna < 3.15: CVE-2026-45409 idna.encode() DoS on crafted long inputs.
# urllib3 < 2.7.0: PYSEC-2026-141/142 cross-origin header leak + decompression bomb.
idna>=3.18
urllib3>=2.7.0
